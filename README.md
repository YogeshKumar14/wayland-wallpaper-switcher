# Wayland Wallpaper Switcher
Wayland Wallpaper Switcher (WWS) is a wallpaper switching utility for wayland linux systems featuring a menu using `rofi` and `wallust` to automatically change your system theme based on your active wallpaper.

## Installation
Clone the repository.
```bash
git clone
```
